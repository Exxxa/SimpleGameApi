# Logging
logging.level.root=info

# Spring Configuration (if using Spring Boot)
spring.application.name=naval-battle-client

# Server Configuration
server.url=http://localhost:8080   # Replace with the actual server URL and port

# HTTP Connection Configuration (if needed)
# client.http.connect-timeout=5000
# client.http.read-timeout=5000
# client.http.max-connections=10

# OpenAPI Configuration
openapi.generator.apiPackage=com.yourcompany.navalbattle.controller
openapi.generator.modelPackage=com.yourcompany.navalbattle.model
openapi.generator.configPackage=com.yourcompany.navalbattle.config
